services:
  api:
    ports: !reset []
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes: !override
      - smartfoodia_receipts:/data/receipts

  web:
    build:
      args:
        NEXT_PUBLIC_API_URL: https://${PUBLIC_DOMAIN}
    ports: !reset []

  worker:
    volumes: !override
      - smartfoodia_receipts:/data/receipts

  db:
    ports: !reset []

  caddy:
    image: caddy:2.10-alpine
    container_name: smartfoodia-caddy
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - smartfoodia_caddy_data:/data
      - smartfoodia_caddy_config:/config
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_started

volumes:
  smartfoodia_caddy_data:
  smartfoodia_caddy_config:
  smartfoodia_receipts:
